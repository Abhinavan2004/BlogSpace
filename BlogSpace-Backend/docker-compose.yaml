version: "3.8"

services:
  # Our PostgreSQL database
  db:
    image: postgres:latest
    ports:
      - "5432:5432"
    restart: always
    environment:
      POSTGRES_USER: Abhinav
      POSTGRES_PASSWORD: Abhi@2004
      POSTGRES_DB: BlogSpace
      TZ: Asia/Kolkata
      PGTZ: Asia/Kolkata


  # Database management interface
  adminer:
    image: adminer:latest
    restart: always
    ports:
      - "8888:8080"
